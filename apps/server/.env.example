# Ruff Term's server uses Yahoo Finance's public endpoints for market data
# and news — no API key required. This file is only for local overrides.

PORT=4000

# Optional. Unlocks live Claude-generated "Ruffer Impact" news reframing.
# Without it, Ruffer Impact falls back to a simple rule-based heuristic.
ANTHROPIC_API_KEY=
