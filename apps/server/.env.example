# Optional: get a free API key at https://polygon.io/dashboard/signup
# Without a key, the server serves deterministic mock data so the app
# still runs end-to-end with zero configuration.
POLYGON_API_KEY=

PORT=4000
